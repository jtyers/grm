#!/bin/sh

set -eu
export PDM_PROJECT=$(dirname "$0")

if [ ${DEBUG:-0} -eq 1 ]; then
    pdm run \
      pudb \
      -m grm._internal.main \
      "$@"
else
    pdm run \
      python -m grm._internal.main \
      "$@"
fi
